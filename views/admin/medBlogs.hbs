
<style>
    /* Main styling for the medicinal information list */

h1 {
  color: #2c3e50;
  margin-bottom: 20px;
  padding-bottom: 10px;
  border-bottom: 2px solid #3498db;
}

/* Table styling */
table {
  width: 70%;
  border-collapse: collapse;
  background-color: white;
  border-radius: 8px;
  overflow: hidden;
  margin: auto;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
  margin-top: 20px;
}

th {
  background-color: #3498db;
  color: white;
  padding: 15px;
  text-align: left;
  font-weight: bold;
}

td {
  padding: 12px 15px;
  border-bottom: 1px solid #ddd;
}

tr:last-child td {
  border-bottom: none;
}

tr:hover {
  background-color: #f5f5f5;
}

/* Status badges */
.status {
  padding: 5px 10px;
  border-radius: 20px;
  font-size: 14px;
  font-weight: 500;
  text-align: center;
  display: inline-block;
  min-width: 80px;
}

.status-active {
  background-color: #e8f5e9;
  color: #2e7d32;
}

.status-pending {
  background-color: #fff8e1;
  color: #f57c00;
}

.status-archived {
  background-color: #efebe9;
  color: #795548;
}

/* Action buttons */
.action-buttons {
  display: flex;
  gap: 5px;
}

button {
  background-color: #3498db;
  color: white;
  border: none;
  padding: 8px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  transition: background-color 0.3s;
}

button:hover {
  background-color: #2980b9;
}

.btn-edit {
  background-color: #f39c12;
}

.btn-edit:hover {
  background-color: #e67e22;
}

.btn-delete {
  background-color: #e74c3c;
}

.btn-delete:hover {
  background-color: #c0392b;
}

/* Search and add button */
.search-container {
  margin-bottom: 20px;
  display: flex;
  gap: 10px;
}

.search-container input {
  flex-grow: 1;
  padding: 10px;
  border: 1px solid #ddd;
  border-radius: 4px;
  font-size: 16px;
}

.add-button {
  margin: 20px 0;
  background-color: #2ecc71;
}

.add-button:hover {
  background-color: #27ae60;
}

/* Responsive design */
@media (max-width: 768px) {
  table {
    display: block;
    overflow-x: auto;
  }
  
  .action-buttons {
    flex-direction: column;
  }
  
  .search-container {
    flex-direction: column;
  }
}

/* Notes styling */
.notes {
  max-width: 300px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Date styling */
.date {
  white-space: nowrap;
}
</style><h1>Medicinal Informations List</h1>

{{!-- <div class="search-container">
  <input type="text" placeholder="Search medicinal information...">
  <button>Search</button>
</div> --}}

{{!-- <button class="add-button">+ Add New Entry</button> --}}

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Descrption</th>
      <th>Tracking ID</th>
      <th>Reports</th>
    </tr>
  </thead>
  <tbody>
    {{#each medBlogs}}
    <tr>
      <td>{{Name}}</td>
      <td class="date">{{data}}</td>
      <td class="notes">{{_id}}</td>
      <td>
        <iframe src="/images/report/{{_id}}.pdf" type="application/pdf" frameborder="0"></iframe>
      </td>
     
    </tr>
    {{/each}}
  </tbody>
</table>